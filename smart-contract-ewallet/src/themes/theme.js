// Global styles. Every other themes will be merged with this.

const baseTheme = (themeName: string) => ({
  // Positive buttons.
  [themeName + '.Button']: {
    backgroundColor: '#000',
    color: '#fff'
  }
  // Header.
});

export default baseTheme;
